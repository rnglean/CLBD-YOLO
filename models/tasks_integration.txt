# Custom module imports for ultralytics/nn/tasks.py

from models.c3k2_gd import C3k2_GhostDynamicConv
from models.lae import LAE
from models.fusion import Fusion
from models.dat import DAttention


# Fusion integration for ultralytics/nn/tasks.py

elif m is Fusion:
    args[0] = d[args[0]]
    c1, c2 = [ch[x] for x in f], (
        sum([ch[x] for x in f])
        if args[0] == 'concat'
        else ch[f[0]]
    )
    args = [c1, args[0]]
# LAE and DAttention integration for ultralytics/nn/tasks.py

elif m in {LAE, DAttention}:
    c2 = ch[f]
    args = [c2, *args]
# C3k2_GhostDynamicConv integration for ultralytics/nn/tasks.py

# Add the custom module to the C3k2 module group.


# Add C3k2_GhostDynamicConv to the existing C3K2_CLASS tuple:
# C3K2_CLASS = (..., C3k2_GhostDynamicConv, ...)


# In parse_model(), C3k2_GhostDynamicConv should be handled
# in the same way as the standard C3k2 module:
#
# 1. Automatically insert input/output channels:
#
#     c1, c2 = ch[f], args[0]
#     c2 = make_divisible(min(c2, max_channels) * width, 8)
#     args = [c1, c2, *args[1:]]
#
# 2. Insert the repeated block number:
#
#     args.insert(2, n)
#     n = 1
