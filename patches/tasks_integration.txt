# CLBD-YOLO custom module integration for ultralytics/nn/tasks.py


# ============================================================
# 1. Add the following imports near the top of tasks.py
# ============================================================

from models.c3k2_gd import C3k2_GhostDynamicConv
from models.lae import LAE
from models.fusion import Fusion
from models.dat import DAttention


# ============================================================
# 2. Add C3k2_GhostDynamicConv to the existing C3K2_CLASS tuple
# ============================================================

# Example:
#
# C3K2_CLASS = (
#     ...,
#     C3k2_GhostDynamicConv,
#     ...
# )
#
# Do not replace the original C3K2_CLASS tuple.
# Only add C3k2_GhostDynamicConv to the existing tuple.


# ============================================================
# 3. Fusion parsing rule
# Add this branch inside parse_model()
# ============================================================

elif m is Fusion:
    args[0] = d[args[0]]
    c1, c2 = [ch[x] for x in f], (
        sum([ch[x] for x in f])
        if args[0] == "concat"
        else ch[f[0]]
    )
    args = [c1, args[0]]


# ============================================================
# 4. LAE and DAttention parsing rule
# Add this branch inside parse_model()
# ============================================================

elif m in {LAE, DAttention}:
    c2 = ch[f]
    args = [c2, *args]


# ============================================================
# 5. C3k2_GhostDynamicConv parsing
# ============================================================

# C3k2_GhostDynamicConv is included in C3K2_CLASS.
# Therefore, the existing C3K2_CLASS parsing logic in tasks.py
# automatically performs:
#
#     c1, c2 = ch[f], args[0]
#     c2 = make_divisible(min(c2, max_channels) * width, 8)
#     args = [c1, c2, *args[1:]]
#
# and:
#
#     args.insert(2, n)
#     n = 1
#
# This makes a YAML definition such as:
#
# - [-1, 2, C3k2_GhostDynamicConv, [256, False, 0.25]]
#
# compatible with:
#
# C3k2_GhostDynamicConv(c1, c2, n, False, 0.25)
